[pytest]
testpaths = tests
filterwarnings =
    error
    # This Python links LibreSSL rather than OpenSSL. True, known, and the reason
    # fetch.atlas14 carries a curl fallback -- not news on every run.
    ignore:.*OpenSSL.*:Warning
