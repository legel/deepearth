# Secrets — never commit
.env

# Large binary raster data — reproducible by running pipeline scripts
**/*.tif
sentinel2/data/s2_*.tif

# WatNet ML model directory — 148 MB of test imagery and model weights
WatNet/

# HTTP cache directories
cache/
OWM_cache/

# Python artifacts
__pycache__/
*.pyc
*.pyo
*.pyd

# OS artifacts
.DS_Store
