# cfx_sr417_corridor — flood digital-twin pipeline
#
# Versions are pinned to the environment this pipeline is verified against (Python 3.11).
# They are not arbitrary: numpy 2.x, pandas 3.x and geopandas 1.x all carry breaking changes
# that rasterio/pysheds are sensitive to, so an unpinned install is not reproducible.
#
#   python3.11 -m venv .venv && .venv/bin/pip install -r requirements.txt

# ── Core numerics and I/O ────────────────────────────────────────────────────────────────
numpy==2.4.6
scipy==1.17.1
pandas==3.0.5
matplotlib==3.11.1
Pillow==12.3.0

# ── Geospatial ───────────────────────────────────────────────────────────────────────────
rasterio==1.4.4
geopandas==1.1.4
shapely==2.1.2
pyproj==3.7.2

# ── Hydrology / terrain services ─────────────────────────────────────────────────────────
py3dep==0.19.0        # USGS 3DEP DEM download
pynhd==0.20.0         # NHDPlus HR hydrography
pysheds==0.5          # D8 flow routing, accumulation, HAND

# ── LiDAR ────────────────────────────────────────────────────────────────────────────────
laspy[lazrs]==2.7.0   # LAZ point-cloud reading

# ── Web ──────────────────────────────────────────────────────────────────────────────────
requests==2.34.2
flask==3.1.3

# ── Optional: DEM conditioning ───────────────────────────────────────────────────────────
# richdem provides the Lindsay (2016) depression-breaching used by dem/dem_hydro.py and the
# geomorphometric derivatives in dem/dem_terrain.py. It is NOT installed by default because it
# fails to build against several modern C++ toolchains (its pybind11 wrapper does not compile
# on recent macOS clang). Everything else in the pipeline runs without it; only re-running
# those two conditioning scripts requires it. Install separately if you need them:
#     pip install richdem
# richdem

# ── Optional: learned-surrogate research (research/) ─────────────────────────────────────
# Not required by the digital-twin pipeline. See research/README.md.
# torch==2.13.0
# torch_geometric
# torch_scatter
