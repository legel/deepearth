BIOLOGICAL PROBE — SCORECARD

NOT YET RUN under the current harness. No records exist in this loop's records/ directory.

This file is the git-visible view of the campaign: current best per metric, its fair gain against the
strongest fair baseline, and the diagnosis. It must be GENERATED from this loop's records, never
hand-written -- see how autoresearch/spacetime/editable_files/harness.py::write_scorecard does it, and
scorecard.md for what each row means.

To bring this loop up to the same standard:
  1. give it a result contract, so a number carries its own identity (capability, mode, split, metric)
  2. give it a record gate, so a score is only compared like-for-like
  3. generate this file after every run

Until then, treat any biological number in this repository as unverified.
